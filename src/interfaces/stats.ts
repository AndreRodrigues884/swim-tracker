export interface WSession { id: string; date: string; day_type: string }
export interface SetLog   { exercise_id: string; weight_kg: number; reps_done: number; session_id: string }
export interface Prog     { date: string; exercise: string; load_kg: number }
